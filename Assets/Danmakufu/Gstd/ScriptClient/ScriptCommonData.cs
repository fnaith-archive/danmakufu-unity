using System.Collections.Generic;
using Gstd.Script;

namespace Gstd
{
    namespace ScriptClient
    {
        sealed class ScriptCommonData
        {
            /*private Dictinary<string, Value> mapValue;

            private Value _ReadRecord(gstd::ByteBuffer &buffer);
            private void _WriteRecord(gstd::ByteBuffer &buffer, Value& comValue);

            public ScriptCommonData();
            virtual ~ScriptCommonData();
            public void Clear();
            public bool IsExists(std::string name);
            public Value GetValue(std::string name);
            public void SetValue(std::string name, Value v);
            public void DeleteValue(std::string name);
            public void Copy(ScriptCommonData dataSrc);
            public List<string> GetKeyList();

            public void ReadRecord(gstd::RecordBuffer& record);
            public void WriteRecord(gstd::RecordBuffer& record);*/
        }
    }
}
